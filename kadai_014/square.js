//引数で渡した値を二乗した計算結果を戻り値として返す関数

const square = (num) => {
    return num * num;
  }
  
  // 関数の戻り値を出力する
  console.log(square(12));



// 合格基準
// 1 アロー関数またはfunctionのいずれかで関数が定義されている
// 2 引数の二乗の計算結果がコンソールに出力されている
